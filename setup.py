"""
setup.py
--------
Legacy fallback for older pip versions that don't support PEP 517/518.
All authoritative metadata lives in pyproject.toml.
"""
from setuptools import setup

if __name__ == "__main__":
    setup()
